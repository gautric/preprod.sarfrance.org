---
inclusion: auto
---

# Python Environment

- Always use the project's virtual environment located at `.venv/`
- Before running any Python command, activate the venv: `source .venv/bin/activate`
- When installing packages, activate the venv first, then use `pip install`
- Never use system Python directly — always ensure the venv is active
